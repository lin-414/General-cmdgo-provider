推进已确认的四项改进（不含 cmdgo_provider.py 拆模块）：

1. EXE 更名 General-cmdgo-provider.exe：build_gui.cmd / cmdgo-gui.spec / release.yml（产物路径+冒烟测试进程名）/ login.py 探测顺序（新名→旧名兼容→run.py）/ start_proxy 脚本 / README 同步更新；本地 dist 清理并按新名重建。

2. 桌面体验三件套：
   - cmdgo_provider.py 增加 APP_VERSION 常量（与 pyproject 同步），/healthz 附 appVersion；
   - GUI「开机自启」开关：winreg 写/删 HKCU Run 键（非 Windows 隐藏），新增 --minimized 参数自启时直接进托盘；
   - 更新提示：后台查 GitHub releases/latest，元组比较版本，有新版显示可点击的黄色提示；
   - 日志落盘：RotatingFileHandler 1MB×3 到 %APPDATA%\cmdgo-provider\logs\app.log。

3. 账号健康可视化：新增 POST /account/test（对指定账号发 16-token 迷你请求，只报结果不记冷却）；GUI 账号行加「测试」按钮 + 点击账号名输出完整详情日志。

4. 账号导出/导入：credstore.py 加 Fernet+PBKDF2 口令加密（cryptography 惰性导入）；pool.py 加 export_accounts/import_accounts（按 key 去重）；GUI 面板加导出/导入按钮；构建依赖加 cryptography；补回环测试。

收尾：全量 pytest（30+）+ ruff 通过；EXE 按新名重建并启动验证；分 4 个主题提交推送 main。不自动发版，完成后可打 v0.6.0 tag 走现有工作流。

不做：自动下载更新、防 CSRF、盲透传 tool_choice/response_format、cmdgo_provider.py 拆模块。